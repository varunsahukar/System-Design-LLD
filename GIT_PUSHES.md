# Git Pushes and Commit Practice

Best practice for this repository:

- Make each meaningful update in a separate commit.
- Push each commit after it is complete so the remote history stays current.
- Use clear commit messages that describe what changed and why.
- Keep pull requests focused and easy to review.
- Verify the remote branch with `git status` before pushing.
